{
    "seed": 1,
    "model": "ResNet",
    "model_params": {},

    "optimizer": "AdamW",
    "optimizer_params": {
        "lr": 1e-2,
        "betas": [0.1, 0.2]
    },

    "epochs": 20,
    "batch_size": 512,

    "checkpoint_folder": "checkpoints/test_resnet_512"
}